ARG NODE_EXPORTER_IMAGE_NAME=${NODE_EXPORTER_IMAGE_NAME}
ARG NODE_EXPORTER_IMAGE_TAG=${NODE_EXPORTER_IMAGE_TAG}
ARG PLATFORM=${PLATFORM}

FROM --platform=${PLATFORM} ${NODE_EXPORTER_IMAGE_NAME}:${NODE_EXPORTER_IMAGE_TAG}
